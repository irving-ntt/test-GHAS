# Archivo de ejemplo de configuración de variables de entorno (general.env.sample)
# Copia este archivo como 'general.env' y reemplaza los valores según sea necesario.

# Nombre del catálogo en Databricks Unity Catalog
CATALOG=<CATALOGO>

# Nombre del esquema dentro del catálogo
SCHEMA=<SCHEMA>

# Alcance del almacén de secretos en Databricks
SCOPE=<SCOPE>

# Usuario para la conexión con la base de datos
CONN_USER=<USUARIO>

# Clave de acceso para la conexión (sensible, reemplazar con un secreto seguro)
CONN_KEY=<CONTRASEÑA>

# External location
EXTERNAL_LOCATION=<uri>

# Instrucciones:
# 1. Reemplaza <CATALOGO>, <SCHEMA>, <SCOPE>, <USUARIO>, <CONTRASEÑA> y <EXTERNAL_LOCATION> con los valores reales.
# 2. Guarda este archivo como 'general.env' antes de ejecutar la aplicación.
# 3. No subas el archivo 'general.env' al repositorio, usa este archivo como plantilla.
